
apiVersion: v1
kind: Service
metadata:
  name: {{ include "application-generator.fullname" . }}
  labels:
    {{- include "application-generator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "application-generator.selectorLabels" . | nindent 4 }}
